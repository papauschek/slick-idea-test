package db

import org.junit.runner.RunWith
import org.scalatest.FunSuite
import org.scalatest.junit.JUnitRunner

@RunWith(classOf[JUnitRunner])
class SlickTests extends FunSuite {

  test("Dummy test") {
    assert(true === true)
  }
}
